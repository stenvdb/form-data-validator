const config = {
  scrollToFirstError: true,
  parentSelector: 'div',
  errorClass: 'error',
  customTypes: [],
  rules: [],
  ignoreFields: [],
  customValidityMessages: []
};

export default config;
